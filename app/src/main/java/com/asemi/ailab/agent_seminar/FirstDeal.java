package com.asemi.ailab.agent_seminar;

import java.util.ArrayList;
import java.util.Random;

/**
 * Created by Wataru on 2017/02/18.
 */

public class FirstDeal {
    ArrayList<StrategyCard> first_hands = new ArrayList<>();


    /*public FirstDeal(AllDeck deck, int max_player){
        ArrayList<Player> players = new ArrayList<>(max_player);
        Random rnd = new Random();

        for(int i=0; i<players.size(); i++){

            players.add(new Player());
        }

        for(int i=0; i<3; i++) {
            this.first_hands.add(deck.strategyDeck.getFirst());
            deck.strategyDeck.removeFirst();
        }

    }*/

}
